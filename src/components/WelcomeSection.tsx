import React from 'react';

interface WelcomeSectionProps {
  onSampleEmailClick: () => void;
}

export const WelcomeSection: React.FC<WelcomeSectionProps> = ({ onSampleEmailClick }) => {
  return null; // Removed for minimal UI - no welcome section needed
};